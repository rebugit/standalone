apiVersion: batch/v1
kind: Job
metadata:
  name: database-migration-job
spec:
  template:
    spec:
      containers:
        - name: {{ .Values.migration.containers.name }}
          image: {{ .Values.migration.containers.image.repository }}:{{ .Values.migration.containers.image.tag }}
          imagePullPolicy: Always
      restartPolicy: Never
  backoffLimit: 4
