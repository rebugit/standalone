apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Values.dashboard.deployment.name }}
  labels:
    app: {{ .Values.dashboard.deployment.name }}
spec:
  replicas: 1
  selector:
    matchLabels:
      app: {{ .Values.dashboard.deployment.name }}
  template:
    metadata:
      labels:
        app: {{ .Values.dashboard.deployment.name }}
    spec:
      containers:
        - name: {{ .Values.dashboard.containers.name }}
          image: {{ .Values.dashboard.containers.image.repository }}:{{ .Values.dashboard.containers.image.tag }}
          imagePullPolicy: Always
          ports:
            - containerPort: {{ .Values.dashboard.containers.port }}
          env:
            - name: REACT_APP_BASE_PATH
              value: /rebugit/web
            {{- if .Values.dashboard.environment}}
            {{- range .Values.dashboard.environment }}
            - name: {{ .name }}
              value: {{ .value }}
            {{- end }}
            {{- end -}}

{{/*          resources:*/}}
{{/*            requests:*/}}
{{/*              memory: "50Mi"*/}}
{{/*              cpu: "500m"*/}}
{{/*            limits:*/}}
{{/*              memory: "500Mi"*/}}
{{/*              cpu: "1000m"*/}}
{{/*          livenessProbe:*/}}
{{/*            httpGet:*/}}
{{/*              port: {{ .Values.dashboard.containers.port }}*/}}
{{/*              path: /*/}}
{{/*            initialDelaySeconds: 30*/}}
{{/*            periodSeconds: 5*/}}